# AI Vision Service environment template
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# Runtime defaults used by Dockerfile/docker-compose.yml
APP_HOST=0.0.0.0
APP_PORT=8000

# Model documented by the service; fallback mode is used if YOLO cannot load.
MODEL_NAME=yolov8n.pt
